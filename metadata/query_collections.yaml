- name: allowed-queries
  definition:
    queries:
      - name: account_balance
        query: |
          query MyQuery ($aid: Int!) {
            pgbench_accounts_by_pk(aid: $aid) {
              abalance
            }
          }
